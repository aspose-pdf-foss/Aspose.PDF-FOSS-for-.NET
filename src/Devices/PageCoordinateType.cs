namespace Aspose.Pdf;

/// <summary>
/// Which page-box the rendered image extents come from. Mirrors
/// <c>Aspose.Pdf.PageCoordinateType</c>.
/// </summary>
public enum PageCoordinateType
{
    /// <summary>Use the page's CropBox (visible area).</summary>
    CropBox,
    /// <summary>Use the page's MediaBox (full physical page).</summary>
    MediaBox,
}
