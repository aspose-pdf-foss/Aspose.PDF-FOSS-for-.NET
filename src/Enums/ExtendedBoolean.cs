namespace Aspose.Pdf;

/// <summary>A tri-state Boolean — True/False plus an Undefined sentinel used at deserialization
/// time when a value isn't carried on the PDF dictionary entry.</summary>
public enum ExtendedBoolean
{
    Undefined = 0,
    True = 1,
    False = 2,
}
