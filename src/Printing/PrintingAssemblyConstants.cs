namespace Aspose.Pdf
{
    /// <summary>
    /// Build-level constants. This half carries the printing dependency pin:
    /// the System.Drawing.Common version this build is validated against, quoted
    /// by the missing-dependency guidance so the message and the pin cannot drift.
    /// </summary>
    internal partial class AssemblyConstants
    {
        /// <summary>The System.Drawing.Common version referenced by the build.</summary>
        internal const string SystemDrawingCommonVersion = "8.0.0";
    }
}
